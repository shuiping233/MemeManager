# 仓库根 .editorconfig —— 按目录层级自动发现，对 MemeManager 与 MemeManager.Tests 同时生效
root = true

[*.cs]
# 编码：统一 UTF-8 无 BOM（VS/Rider/VS Code 保存 cs 文件时按此编码写入）
charset = utf-8
# 行尾：统一 LF（现状 50/51 为 LF）
end_of_line = lf
# 缩进：4 空格（现状全部一致）
indent_style = space
indent_size = 4
# 文件末尾保证一个换行 / 保存时去掉行尾空格
insert_final_newline = true
trim_trailing_whitespace = true

# ---- C# 代码风格规则 ----
# severity 一律 suggestion：保存 / Code Cleanup 时自动应用，不参与 build 报错
[*.cs]
# using 排序：System 命名空间排最前
dotnet_sort_system_directives_first = true

# 不使用 this. 限定符（与现状一致：全项目 0 处 this.）
dotnet_style_qualification_for_field = false:suggestion
dotnet_style_qualification_for_property = false:suggestion
dotnet_style_qualification_for_method = false:suggestion
dotnet_style_qualification_for_event = false:suggestion

# 使用语言关键字而非 BCL 类型（string 而非 String）
dotnet_style_predefined_type_for_locals_parameters_members = true:suggestion
dotnet_style_predefined_type_for_member_access = true:suggestion

# 现代 C# 惯用法
dotnet_style_object_initializer = true:suggestion
dotnet_style_collection_initializer = true:suggestion
dotnet_style_coalesce_operator = true:suggestion
dotnet_style_null_propagation = true:suggestion
dotnet_style_prefer_conditional_expression_over_assignment = true:suggestion

# file-scoped namespace（与现状一致：100% file-scoped）
csharp_style_namespace_declarations = file_scoped:suggestion
